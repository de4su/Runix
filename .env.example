# Piston API auth token (required as of Feb 15, 2026).
# Request a token from EngineerMan on Discord (for non-commercial/educational use).
# Without this, the app falls back to its cached version list and execution may be blocked.
VITE_PISTON_KEY="your_token_here"
